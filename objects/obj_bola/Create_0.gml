/// @description Iniciando a movimentação
velocidade = 2;
incremento_velocidade = 0.5;

speed = 0;

randomise();
direction = choose(45, 135, 225, 315);

alarm[0] = 60; // 1 segundo para iniciar a bola